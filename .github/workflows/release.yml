name: Build and Release Extension

on:
  push:
    tags:
      - 'v[0-9].[0-9]*.[0-9]*'

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    container: alpine:latest
    permissions:
      contents: write

    steps:
      - name: Install dependencies (Alpine)
        run: |
          apk add --no-cache git zip jq bash sed nodejs npm

      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure git safe directory
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Validate tag
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          if [[ ! "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Invalid tag format: $TAG"
            exit 1
          fi

      - name: Update Manifest Version
        run: |
          NEW_VERSION=$(echo ${{ github.ref_name }} | sed 's/^v//')
          jq ".version = \"$NEW_VERSION\"" manifest.json > manifest.tmp && mv manifest.tmp manifest.json

      - name: Update Package Version
        run: |
          NEW_VERSION=$(echo "${{ github.ref_name }}" | sed 's/^v//')
          jq ".version = \"$NEW_VERSION\"" package.json > package.tmp && mv package.tmp package.json

      - name: Update README.md Version Badge
        run: |
          NEW_VERSION=$(echo ${{ github.ref_name }} | sed 's/^v//')
          # This replaces the version number inside the Shields.io badge URL
          sed -i "s/version-[0-9]\+\.[0-9]\+\.[0-9]\+-informational/version-$NEW_VERSION-informational/g" README.md

      - name: Generate Release Notes
        run: |
          chmod +x generate-changelog.sh
          ./generate-changelog.sh
          test -s RELEASE_NOTES.md
          grep -q "${{ github.ref_name }}" RELEASE_NOTES.md

      - name: Update Cumulative CHANGELOG.md
        run: |
          NEW=$(cat RELEASE_NOTES.md)
          if [ -f CHANGELOG.md ]; then
            HEADER=$(awk '/^## /{exit} {print}' CHANGELOG.md)
            BODY=$(awk '/^## /{found=1} found{print}' CHANGELOG.md)
            printf '%s\n\n%s\n\n%s\n' "$HEADER" "$NEW" "$BODY" > CHANGELOG.md
          else
            printf '# Changelog\n\nAll notable changes to this project will be documented in this file.\n\n%s\n' "$NEW" > CHANGELOG.md
          fi

      - name: Commit and Push Version Bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add manifest.json package.json README.md CHANGELOG.md
          git commit -m "chore: bump version to ${{ github.ref_name }} [skip ci]"
          git push origin HEAD:main

      - name: Install npm dependencies
        run: npm ci

      - name: Build extension
        run: npm run build

      - name: Run Packaging Script
        run: |
          chmod +x package.sh
          ./package.sh
          test -s dist/ask-gemini-extension.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ github.ref_name }}
          body_path: RELEASE_NOTES.md
          prerelease: ${{ contains(github.ref_name, '-') }}
          files: |
            dist/ask-gemini-extension.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
